server:
  port: 7001
eureka:
  instance:
    hostname: eureka7001.com # 服务端的实例名
  client:
    register-with-eureka: false # false 是否注册到eureka
    fetch-registry: false # false 是否需要去搜索注册中心
    service-url:
      defaultZone: http://eureka7002.com:7002/eureka/